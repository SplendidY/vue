import { createApp } from 'vue'
import App from './App.vue'
// import Layerselector from './components/Layerselector.vue'
// window.viewer = null;
// createApp(App).mount('#app')
const app = createApp(App)
app.mount('#app')

// app.component('Layerselector', Layerselector)